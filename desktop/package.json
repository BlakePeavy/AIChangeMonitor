{
  "name": "change-monitor-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Tauri 2 desktop shell for AI Change Monitor (wraps the Go engine)",
  "scripts": {
    "sidecar": "python3 scripts/build-sidecar.py",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
